.DS_Store
__pycache__/
*.pyc
*.pyo
.env
# local auto-post credentials — never commit
scripts/*.env
scripts/client_secret.json
scripts/token.json
node_modules/

.jpg
.png
v2
# v1 page backups from the v2 redesign — keep local, never deploy
*.v1-backup.html
# local-only visual review/markup tool — never deploy
assets/review-mode.js